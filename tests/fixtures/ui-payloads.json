{
  "/api/me": {
    "email": "dana@acme.test",
    "subStatus": null,
    "plan": "free",
    "monthlyRunsUsed": 0,
    "monthlyRunsLimit": 5,
    "active": false,
    "reason": "free_plan",
    "currentPeriodEnd": null,
    "isAdmin": true,
    "displayName": "Dana Reyes",
    "avatarUrl": null,
    "initials": "DR"
  },
  "/api/account": {
    "profile": {
      "userId": "usr_fixture",
      "email": "dana@acme.test",
      "displayName": "Dana Reyes",
      "avatarUrl": null,
      "initials": "DR",
      "authMethod": null,
      "authMethodKnown": false,
      "createdAt": 1782086400,
      "pendingEmailChange": null
    },
    "org": {
      "orgId": "org_fixture",
      "name": "Acme Corporation Holdings International",
      "role": "owner",
      "tier": null,
      "plan": "free",
      "subStatus": null,
      "seatsPurchased": 1,
      "seatsUsed": 2,
      "billingEmail": null,
      "hasStripeCustomer": false
    },
    "entitlement": {
      "active": false,
      "reason": "free_plan",
      "currentPeriodEnd": null
    },
    "usage": {
      "monthlyRunsUsed": 0,
      "monthlyRunsLimit": 5
    },
    "credit": {
      "balanceCents": 0,
      "balance": "$0.00",
      "unsyncedCents": 0,
      "expiringCents": 0,
      "expiringAt": null,
      "known": true
    },
    "capabilities": {
      "branding": {
        "unlocked": false,
        "requiredTier": "firm"
      },
      "team": {
        "canManage": true
      },
      "apiKeys": {
        "canManage": true
      },
      "dangerZone": {
        "canDeleteOrg": true
      },
      "billing": {
        "canManage": true
      }
    }
  },
  "/api/account/profile": {
    "profile": {
      "userId": "usr_fixture",
      "email": "dana@acme.test",
      "displayName": "Dana Reyes",
      "avatarUrl": null,
      "initials": "DR",
      "authMethod": null,
      "authMethodKnown": false,
      "createdAt": 1782086400,
      "pendingEmailChange": null
    },
    "org": {
      "orgId": "org_fixture",
      "name": "Acme Corporation Holdings International",
      "role": "owner",
      "tier": null,
      "plan": "free",
      "subStatus": null,
      "seatsPurchased": 1,
      "seatsUsed": 2,
      "billingEmail": null,
      "hasStripeCustomer": false
    },
    "entitlement": {
      "active": false,
      "reason": "free_plan",
      "currentPeriodEnd": null
    },
    "usage": {
      "monthlyRunsUsed": 0,
      "monthlyRunsLimit": 5
    },
    "credit": {
      "balanceCents": 0,
      "balance": "$0.00",
      "unsyncedCents": 0,
      "expiringCents": 0,
      "expiringAt": null,
      "known": true
    },
    "capabilities": {
      "branding": {
        "unlocked": false,
        "requiredTier": "firm"
      },
      "team": {
        "canManage": true
      },
      "apiKeys": {
        "canManage": true
      },
      "dangerZone": {
        "canDeleteOrg": true
      },
      "billing": {
        "canManage": true
      }
    }
  },
  "/api/account/sessions": {
    "sessions": [],
    "complete": true,
    "indexedOnly": true,
    "note": "Sessions issued before device tracking shipped are not listed. Sessions expire after 30 days."
  },
  "/api/account/logins": {
    "logins": [],
    "since": "Recorded from the release that added sign-in history. Earlier sign-ins were not logged."
  },
  "/api/account/notifications": {
    "groups": [
      {
        "id": "billing",
        "title": "Billing",
        "description": "Money and access. Email cannot be switched off for the first two — a missed decline becomes a lapsed account.",
        "rows": [
          {
            "id": "pay_failed",
            "label": "Payment failed",
            "hint": "Sent on each Stripe retry, with the deadline.",
            "channels": {
              "email": {
                "on": true,
                "locked": true
              },
              "inapp": {
                "on": true,
                "locked": false
              },
              "slack": {
                "on": false,
                "locked": false
              }
            }
          },
          {
            "id": "plan_changed",
            "label": "Plan or seats changed",
            "hint": "Upgrade, downgrade, seat add-on, cancellation.",
            "channels": {
              "email": {
                "on": true,
                "locked": true
              },
              "inapp": {
                "on": false,
                "locked": false
              },
              "slack": {
                "on": false,
                "locked": false
              }
            }
          },
          {
            "id": "invoice_paid",
            "label": "Invoice paid",
            "hint": "Receipt to your billing email.",
            "channels": {
              "email": {
                "on": true,
                "locked": false
              },
              "inapp": {
                "on": false,
                "locked": false
              },
              "slack": {
                "on": false,
                "locked": false
              }
            }
          }
        ]
      },
      {
        "id": "product",
        "title": "Product",
        "description": "Scans and findings. Silence any of these without losing billing notices.",
        "rows": [
          {
            "id": "scan_done",
            "label": "Scan complete",
            "hint": "Every finished run, CI or manual.",
            "channels": {
              "email": {
                "on": false,
                "locked": false
              },
              "inapp": {
                "on": true,
                "locked": false
              },
              "slack": {
                "on": false,
                "locked": false
              }
            }
          },
          {
            "id": "severity",
            "label": "New finding at or above high",
            "hint": "Threshold: high. Critical always notifies.",
            "channels": {
              "email": {
                "on": true,
                "locked": false
              },
              "inapp": {
                "on": true,
                "locked": false
              },
              "slack": {
                "on": true,
                "locked": false
              }
            }
          },
          {
            "id": "monitor",
            "label": "Monitor alerts",
            "hint": "A scheduled sweep failed, or a delta appeared.",
            "channels": {
              "email": {
                "on": true,
                "locked": false
              },
              "inapp": {
                "on": false,
                "locked": false
              },
              "slack": {
                "on": false,
                "locked": false
              }
            }
          }
        ]
      }
    ],
    "slack": {
      "configured": false,
      "note": "No Slack webhook is configured for this organisation, so Slack toggles have nowhere to deliver."
    },
    "stored": true
  },
  "/api/billing/summary": {
    "org": {
      "orgId": "org_fixture",
      "name": "Acme Corporation Holdings International",
      "role": "owner",
      "tier": null,
      "plan": "free",
      "subStatus": null,
      "seatsPurchased": 1,
      "seatsUsed": 2
    },
    "entitlement": {
      "active": false,
      "reason": "free_plan",
      "currentPeriodEnd": null
    },
    "billingEmail": {
      "address": "dana@acme.test",
      "explicit": false
    },
    "credit": {
      "balanceCents": 0,
      "balance": "$0.00",
      "known": true
    },
    "subscription": null,
    "paymentMethod": null,
    "billingAddress": null,
    "reason": "no_stripe_customer",
    "message": "This organisation has never been through checkout, so there is nothing on file with Stripe yet."
  },
  "/api/billing/invoices": {
    "invoices": [],
    "reason": "no_stripe_customer",
    "message": "No invoice has been raised yet — this organisation has not been through checkout."
  },
  "/api/keys": {
    "keys": []
  },
  "/api/org": {
    "org": {
      "orgId": "org_fixture",
      "name": "Acme Corporation Holdings International",
      "plan": "free",
      "subStatus": null,
      "seatsPurchased": 1,
      "seatsUsed": 2,
      "tier": null
    },
    "branding": {
      "companyName": null,
      "logoUrl": null,
      "accent": null,
      "available": false,
      "appliesToNewReports": false,
      "domain": {
        "hostname": null,
        "status": null,
        "detail": null,
        "checkedAt": null,
        "attempts": 0
      }
    },
    "role": "owner",
    "members": [
      {
        "userId": "usr_fixture",
        "email": "dana@acme.test",
        "role": "owner",
        "joinedAt": 1782086400
      },
      {
        "userId": "usr_second",
        "email": "engineering-oncall@acme.test",
        "role": "member",
        "joinedAt": 1784678400
      }
    ],
    "pendingInvites": []
  },
  "/api/account/org": {
    "org": {
      "orgId": "org_fixture",
      "name": "Acme Corporation Holdings International",
      "plan": "free",
      "subStatus": null,
      "seatsPurchased": 1,
      "seatsUsed": 2,
      "tier": null
    },
    "branding": {
      "companyName": null,
      "logoUrl": null,
      "accent": null,
      "available": false,
      "appliesToNewReports": false,
      "domain": {
        "hostname": null,
        "status": null,
        "detail": null,
        "checkedAt": null,
        "attempts": 0
      }
    },
    "role": "owner",
    "members": [
      {
        "userId": "usr_fixture",
        "email": "dana@acme.test",
        "role": "owner",
        "joinedAt": 1782086400
      },
      {
        "userId": "usr_second",
        "email": "engineering-oncall@acme.test",
        "role": "member",
        "joinedAt": 1784678400
      }
    ],
    "pendingInvites": []
  },
  "/api/referrals": {
    "code": "acme-referral-code",
    "link": "https://algosize.com/api/r/acme-referral-code",
    "usage": {
      "used": 0,
      "limit": 25,
      "windowEndsAt": 1817078400,
      "limitReached": false
    },
    "referrals": [],
    "credit": {
      "balanceCents": 0,
      "balance": "$0.00",
      "expiringCents": 0,
      "expiring": null,
      "expiringAt": null,
      "unsyncedCents": 0,
      "known": true,
      "events": []
    },
    "terms": {
      "creditPerReferral": "$120.00",
      "creditPerReferralCents": 12000,
      "signupLimit": 25,
      "earnRule": "You earn $120.00 in credit when a referred organisation's first invoice is paid — not at signup, and not for a trial that never converts.",
      "cashPolicy": "Credit reduces your Algosize bill. It is not withdrawable as cash and cannot be transferred.",
      "expiry": "Earned credit is valid for 12 months from the day it is issued."
    }
  },
  "/api/ci/snippet": {
    "filename": ".github/workflows/algosize-audit.yml",
    "secretName": "ALGOSIZE_API_KEY",
    "setupSteps": [
      "Create an API key in the dashboard (API keys → Create key). Copy it — it is shown once.",
      "Add it to the repository as a secret named ALGOSIZE_API_KEY: gh secret set ALGOSIZE_API_KEY --body '<the key>'",
      "Commit the workflow file below."
    ],
    "workflow": "name: Algosize dependency audit\n\non:\n  pull_request:\n  push:\n    branches: [main, master]\n  schedule:\n    # Weekly, so a dependency that becomes vulnerable between pushes is still\n    # caught. New advisories are published against code that has not changed.\n    - cron: \"0 6 * * 1\"\n\npermissions:\n  contents: read\n  # Required to upload the SARIF report to the Security tab.\n  security-events: write\n  # Required for the sticky PR comment.\n  pull-requests: write\n\njobs:\n  audit:\n    runs-on: ubuntu-latest\n    steps:\n      - uses: actions/checkout@v4\n\n      # Everything below is skipped when the secret is absent, and the job\n      # passes with a notice instead of failing. A workflow that goes red the\n      # moment it is pasted — before the key exists — reads as \"this product\n      # is broken\", and the first thing anyone does with a red required check\n      # is delete it. The same guard is why this file can live in the Algosize\n      # repo itself before the secret is provisioned.\n      - name: Check for the API key\n        id: key\n        env:\n          ALGOSIZE_API_KEY: ${{ secrets.ALGOSIZE_API_KEY }}\n        run: |\n          if [ -n \"$ALGOSIZE_API_KEY\" ]; then\n            echo \"present=true\" >> \"$GITHUB_OUTPUT\"\n          else\n            echo \"present=false\" >> \"$GITHUB_OUTPUT\"\n            echo \"::notice::ALGOSIZE_API_KEY is not set — skipping the dependency audit. Add the secret to enable it.\"\n          fi\n\n      # Collects BOTH analyzers' inputs in one pass, so the pipeline makes a\n      # single request and the dashboard gets a dependency audit and an\n      # Architecture X-ray from the same commit.\n      #\n      # Everything starts from `git ls-files`, which is the whole trick: it\n      # lists tracked files only, so build output, node_modules and anything\n      # else gitignored is excluded without maintaining a denylist that would\n      # rot. Nothing here reaches outside the checkout.\n      - name: Collect lockfiles and architecture inputs\n        id: collect\n        if: steps.key.outputs.present == 'true'\n        run: |\n          python3 - <<'PY'\n          import json, os, subprocess\n\n          LOCK_NAMES = {\"package-lock.json\", \"yarn.lock\", \"requirements.txt\",\n                        \"Gemfile.lock\", \"go.sum\"}\n          # What the architecture analyzer actually parses: service topology\n          # from compose/wrangler/Terraform/k8s, plus source for the import\n          # edges between them.\n          CONFIG_NAMES = {\"wrangler.toml\", \"package.json\", \"_config.yml\", \"_config.yaml\",\n                          \"docker-compose.yml\", \"docker-compose.yaml\",\n                          \"compose.yml\", \"compose.yaml\"}\n          CONFIG_SUFFIX = (\".tf\", \".yml\", \".yaml\")\n          SOURCE_SUFFIX = (\".js\", \".mjs\", \".cjs\", \".jsx\", \".ts\", \".tsx\")\n\n          # Below the server's own caps, so a repo that is merely large gets a\n          # complete answer and only a genuinely huge one gets a flagged\n          # partial. The server reports its own truncation independently.\n          MAX_FILES, MAX_TOTAL, MAX_ONE = 1500, 10 * 1024 * 1024, 512 * 1024\n\n          tracked = subprocess.run([\"git\", \"ls-files\"],\n                                   capture_output=True, text=True).stdout.split()\n\n          def read(p):\n              try:\n                  with open(p, encoding=\"utf-8\") as fh:\n                      return fh.read()\n              except (OSError, UnicodeDecodeError):\n                  return None\n\n          # .env files are never sent. The analyzer does scan them for\n          # hardcoded secrets, but a CI job shipping a real .env to any API is\n          # a worse trade than the finding is worth — and a committed .env is\n          # a problem to fix at the source, not to inventory remotely.\n          def is_env(p):\n              b = os.path.basename(p)\n              return b == \".env\" or b.startswith(\".env.\")\n\n          def is_config(p):\n              b = os.path.basename(p)\n              return (b in CONFIG_NAMES\n                      or b.startswith(\"Dockerfile\")\n                      or p.endswith(CONFIG_SUFFIX))\n\n          locks = []\n          for p in tracked:\n              if os.path.basename(p) in LOCK_NAMES:\n                  c = read(p)\n                  if c is not None:\n                      locks.append({\"path\": p, \"content\": c})\n\n          candidates = [p for p in tracked if not is_env(p)]\n          # Configs first: they carry the topology. If a budget runs out it\n          # should cost import edges, never whole services.\n          ordered = ([p for p in candidates if is_config(p)]\n                     + [p for p in candidates if p.endswith(SOURCE_SUFFIX)])\n\n          arch, total, truncated = [], 0, False\n          for p in ordered:\n              if len(arch) >= MAX_FILES:\n                  truncated = True\n                  break\n              c = read(p)\n              if c is None:\n                  continue\n              n = len(c.encode(\"utf-8\"))\n              if n > MAX_ONE:\n                  truncated = True\n                  continue\n              if total + n > MAX_TOTAL:\n                  truncated = True\n                  break\n              total += n\n              arch.append({\"path\": p, \"content\": c})\n\n          with open(\"payload.json\", \"w\", encoding=\"utf-8\") as fh:\n              json.dump({\n                  \"repo\": os.environ[\"GITHUB_REPOSITORY\"],\n                  \"ref\": os.environ[\"GITHUB_REF\"],\n                  \"commit_sha\": os.environ[\"GITHUB_SHA\"],\n                  \"fail_on\": \"high\",\n                  \"arch_fail_on\": \"none\",\n                  \"lockfiles\": locks,\n                  \"files\": arch,\n              }, fh)\n\n          with open(os.environ[\"GITHUB_OUTPUT\"], \"a\", encoding=\"utf-8\") as out:\n              out.write(\"lockfiles=%d\\n\" % len(locks))\n              out.write(\"archfiles=%d\\n\" % len(arch))\n          if truncated:\n              print(\"::notice::Architecture inputs were capped at %d files / %d MB; \"\n                    \"the analysis will report itself as partial.\"\n                    % (MAX_FILES, MAX_TOTAL // 1024 // 1024))\n          print(\"Collected %d lockfile(s) and %d architecture input(s).\"\n                % (len(locks), len(arch)))\n          PY\n\n      - name: Run the Algosize audit\n        if: steps.key.outputs.present == 'true' && (steps.collect.outputs.lockfiles != '0' || steps.collect.outputs.archfiles != '0')\n        id: audit\n        run: |\n          HTTP=$(curl -sS -o response.json -w '%{http_code}' \\\n            -X POST \"https://algosize.com/api/ci/runs\" \\\n            -H \"Authorization: Bearer ${{ secrets.ALGOSIZE_API_KEY }}\" \\\n            -H \"Content-Type: application/json\" \\\n            --data @payload.json)\n          if [ \"$HTTP\" != \"200\" ]; then\n            echo \"::error::Algosize returned HTTP $HTTP\"\n            cat response.json\n            exit 1\n          fi\n          {\n            echo \"run_id=$(jq -r '.runId // empty' response.json)\"\n            echo \"failed=$(jq -r '.failed' response.json)\"\n            echo \"report_url=$(jq -r '.reportUrl // empty' response.json)\"\n            echo \"arch_run_id=$(jq -r '.architecture.runId // empty' response.json)\"\n          } >> \"$GITHUB_OUTPUT\"\n          # The dependency table only exists when lockfiles were submitted; a\n          # repo with none still gets an architecture summary rather than an\n          # empty comment claiming zero advisories.\n          : > table.md\n          if [ \"$(jq -r 'has(\"summary\") and (.runId != null)' response.json)\" = \"true\" ]; then\n            jq -r '\"| Severity | Count |\\n|---|---|\\n| Critical | \\(.summary.critical) |\\n| High | \\(.summary.high) |\\n| Medium | \\(.summary.medium) |\\n| Low | \\(.summary.low) |\"' response.json >> table.md\n          fi\n          if [ \"$(jq -r 'has(\"architecture\")' response.json)\" = \"true\" ]; then\n            {\n              echo \"\"\n              jq -r '\"**Architecture X-ray** · \\(.architecture.summary.clusters) clusters · \\(.architecture.summary.nodes) services · \\(.architecture.summary.findings) findings (\\(.architecture.summary.bySeverity.critical) critical, \\(.architecture.summary.bySeverity.high) high)\"' response.json\n              if [ \"$(jq -r '.architecture.summary.complete' response.json)\" != \"true\" ]; then\n                echo \"\"\n                echo \"_Coverage was partial — some files were skipped or capped, so these counts are a lower bound._\"\n              fi\n            } >> table.md\n          fi\n\n      - name: Download the SARIF report\n        if: steps.audit.outputs.run_id != ''\n        run: |\n          curl -sS -f -o algosize.sarif \\\n            -H \"Authorization: Bearer ${{ secrets.ALGOSIZE_API_KEY }}\" \\\n            \"https://algosize.com/api/runs/${{ steps.audit.outputs.run_id }}/report?format=sarif\"\n\n      - name: Upload to the Security tab\n        if: steps.audit.outputs.run_id != ''\n        uses: github/codeql-action/upload-sarif@v3\n        with:\n          sarif_file: algosize.sarif\n          category: algosize\n\n      - name: Comment on the pull request\n        if: github.event_name == 'pull_request' && steps.audit.outputs.run_id != ''\n        uses: actions/github-script@v7\n        with:\n          script: |\n            const fs = require('fs');\n            const table = fs.readFileSync('table.md', 'utf8');\n            // One sticky comment per PR, updated in place. Appending a new\n            // comment every push buries the conversation and trains people to\n            // collapse the bot.\n            const MARKER = '<!-- algosize-audit -->';\n            const body = [\n              MARKER,\n              '### Algosize dependency audit',\n              '',\n              table,\n              '',\n              '[View the full report](${{ steps.audit.outputs.report_url }})',\n            ].join('\\n');\n            const { data: comments } = await github.rest.issues.listComments({\n              owner: context.repo.owner, repo: context.repo.repo,\n              issue_number: context.issue.number, per_page: 100,\n            });\n            const existing = comments.find((c) => c.body && c.body.includes(MARKER));\n            if (existing) {\n              await github.rest.issues.updateComment({\n                owner: context.repo.owner, repo: context.repo.repo,\n                comment_id: existing.id, body,\n              });\n            } else {\n              await github.rest.issues.createComment({\n                owner: context.repo.owner, repo: context.repo.repo,\n                issue_number: context.issue.number, body,\n              });\n            }\n\n      # Trips on the dependency audit's 'high' threshold, and on the\n      # architecture analysis only if arch_fail_on is set to something other\n      # than \"none\" (currently 'none') — a design finding should not\n      # break a build unless someone chose that.\n      - name: Fail the build on new findings\n        if: steps.audit.outputs.failed == 'true'\n        run: |\n          echo \"::error::Algosize found findings at or above the configured threshold (dependencies: 'high', architecture: 'none'). See ${{ steps.audit.outputs.report_url }}\"\n          exit 1\n"
  },
  "/api/ci/optimizer-snippet": {
    "filename": ".github/workflows/algosize-optimizer.yml",
    "configFilename": "optimizer.config.json",
    "secretName": "ALGOSIZE_API_KEY",
    "setupSteps": [
      "Create an API key in the dashboard (API keys → Create key) and add it as the ALGOSIZE_API_KEY repository secret — the same key the dependency audit uses.",
      "Commit optimizer.config.json at the repo root, naming the self-contained functions to watch (example below).",
      "Commit the workflow file below."
    ],
    "configExample": "{\n  \"$comment\": \"Each entry names one SELF-CONTAINED function: no imports, no closures over file-level helpers. `baseline` is a CEILING, not an expectation — set it one bucket above the true complexity so timing noise cannot fail a build; the check exists to catch real regressions like O(n) -> O(n^2). `file` is repo-root-relative.\",\n  \"entries\": [\n    {\n      \"name\": \"example-sum\",\n      \"file\": \"src/math.js\",\n      \"functionName\": \"sum\",\n      \"sampleInput\": [\n        3,\n        1,\n        4,\n        1,\n        5\n      ],\n      \"baseline\": \"O(n log n)\"\n    }\n  ]\n}",
    "workflow": "name: Algosize algorithm optimizer\n\n# Big-O regression gate. Reads optimizer.config.json, slices each named\n# function out of its file, and asks the Algosize API to measure it — the\n# same analyzer behind the dashboard's Algorithm optimizer and the scheduled\n# monitors' nightly pass, so all three always agree about a function.\n#\n# The build fails only when a measured complexity lands ABOVE the entry's\n# declared baseline ceiling. A function that cannot be found or measured is\n# reported and skipped, not failed: a red build about config is how the gate\n# gets deleted.\n\non:\n  pull_request:\n\npermissions:\n  contents: read\n\njobs:\n  optimizer:\n    runs-on: ubuntu-latest\n    steps:\n      - uses: actions/checkout@v4\n\n      # Skipped-with-notice when the secret is absent, same as the audit\n      # workflow: a workflow that goes red the moment it is pasted reads as\n      # \"this product is broken\".\n      - name: Check for the API key\n        id: key\n        env:\n          ALGOSIZE_API_KEY: ${{ secrets.ALGOSIZE_API_KEY }}\n        run: |\n          if [ -n \"$ALGOSIZE_API_KEY\" ]; then\n            echo \"present=true\" >> \"$GITHUB_OUTPUT\"\n          else\n            echo \"present=false\" >> \"$GITHUB_OUTPUT\"\n            echo \"::notice::ALGOSIZE_API_KEY is not set — skipping the optimizer gate. Add the secret to enable it.\"\n          fi\n\n      - name: Audit configured functions\n        if: steps.key.outputs.present == 'true'\n        env:\n          ALGOSIZE_API_KEY: ${{ secrets.ALGOSIZE_API_KEY }}\n        run: |\n          # acorn does the function slicing — a real parse, not a regex.\n          npm install --no-save --no-audit --no-fund acorn >/dev/null 2>&1\n          node - <<'NODE'\n          const fs = require(\"fs\");\n          const acorn = require(\"acorn\");\n\n          // Both spellings of the polynomial labels rank identically: the API\n          // measures \"O(n²)\" while config ceilings are typed \"O(n^2)\". Labels\n          // past O(n³) carry a raw exponent (\"O(n^4.2)\") and rank by it;\n          // anything unparseable — \"unknown\" included — ranks worst.\n          const RANKS = new Map([\n            [\"O(1)\", 0], [\"O(log n)\", 1], [\"O(n)\", 2], [\"O(n log n)\", 3],\n            [\"O(n²)\", 4], [\"O(n^2)\", 4], [\"O(n³)\", 5], [\"O(n^3)\", 5],\n          ]);\n          const rank = (l) => {\n            if (RANKS.has(l)) return RANKS.get(l);\n            const m = typeof l === \"string\" ? l.match(/^O\\(n\\^([0-9.]+)\\)$/) : null;\n            if (m && Number.parseFloat(m[1]) > 3) return Math.min(5 + (Number.parseFloat(m[1]) - 3), 99);\n            return 100;\n          };\n\n          function extractFunction(source, name) {\n            const ast = acorn.parse(source, { ecmaVersion: \"latest\", sourceType: \"module\" });\n            for (const node of ast.body) {\n              const fn = node.type === \"FunctionDeclaration\" ? node\n                : (node.type === \"ExportNamedDeclaration\" || node.type === \"ExportDefaultDeclaration\")\n                  && node.declaration && node.declaration.type === \"FunctionDeclaration\"\n                  ? node.declaration : null;\n              if (fn && fn.id && fn.id.name === name) return source.slice(fn.start, fn.end);\n            }\n            return null;\n          }\n\n          async function main() {\n            if (!fs.existsSync(\"optimizer.config.json\")) {\n              console.log(\"::notice::optimizer.config.json not found — nothing to audit.\");\n              return;\n            }\n            const config = JSON.parse(fs.readFileSync(\"optimizer.config.json\", \"utf8\"));\n            const entries = Array.isArray(config.entries) ? config.entries : [];\n            let failed = 0;\n\n            for (const entry of entries) {\n              const label = entry.name || entry.functionName || \"unnamed\";\n              if (!entry.file || !entry.functionName) {\n                console.log(\"::warning::\" + label + \": entry missing file or functionName — skipped\");\n                continue;\n              }\n              if (!fs.existsSync(entry.file)) {\n                console.log(\"::warning::\" + label + \": \" + entry.file + \" not found — skipped\");\n                continue;\n              }\n              const code = extractFunction(fs.readFileSync(entry.file, \"utf8\"), entry.functionName);\n              if (!code) {\n                console.log(\"::warning::\" + label + \": function \" + entry.functionName + \" not found in \" + entry.file + \" — skipped\");\n                continue;\n              }\n\n              const res = await fetch(\"https://algosize.com/api/analyze/algo\", {\n                method: \"POST\",\n                headers: {\n                  \"content-type\": \"application/json\",\n                  authorization: \"Bearer \" + process.env.ALGOSIZE_API_KEY,\n                },\n                body: JSON.stringify({ code, sampleInput: entry.sampleInput }),\n              });\n              const body = await res.json().catch(() => ({}));\n              if (!res.ok) {\n                console.log(\"::warning::\" + label + \": API \" + res.status + \" (\" + (body.error || \"error\") + \") — skipped\");\n                continue;\n              }\n              const measured = body.bigO && body.bigO.label || \"unknown\";\n              const ceiling = entry.baseline || null;\n              if (ceiling && rank(measured) > rank(ceiling)) {\n                console.log(\"::error::\" + label + \": measured \" + measured + \" exceeds the declared ceiling \" + ceiling);\n                failed++;\n              } else {\n                console.log(label + \": \" + measured + (ceiling ? \" (ceiling \" + ceiling + \")\" : \"\"));\n              }\n            }\n            if (failed > 0) process.exit(1);\n          }\n          main().catch((err) => { console.log(\"::error::\" + err.message); process.exit(1); });\n          NODE\n"
  },
  "/api/ci/estimate-snippet": {
    "filename": ".github/workflows/algosize-estimate.yml",
    "configFilename": "algosize.budget.json",
    "secretName": "ALGOSIZE_API_KEY",
    "setupSteps": [
      "Use the same ALGOSIZE_API_KEY repository secret the dependency audit uses — there is nothing new to create.",
      "Commit algosize.budget.json at the repo root with your monthly ceiling, or leave it out to annotate without ever failing a build.",
      "Commit the workflow file below."
    ],
    "configExample": "{\n  \"$comment\": \"monthlyCeilingUsd is the number that fails a build, checked against the CHEAPEST priced provider. Omit it (or set it to null) to annotate the pull request without ever failing — which is the right setting until you have watched a few runs and trust the figure. `compose` is repo-root-relative.\",\n  \"compose\": \"docker-compose.yml\",\n  \"monthlyCeilingUsd\": null,\n  \"providers\": [\n    \"hetzner\",\n    \"aws\"\n  ]\n}",
    "workflow": "name: Algosize infrastructure cost\n\n# Prices the committed compose file on every pull request and, when a ceiling\n# is declared, fails the build if the cheapest provider is over it.\n#\n# What this does NOT do, and will not be made to do: connect to a cloud\n# account. There is no credential here and none is accepted. Every figure is\n# a list price applied to what the compose file declares — which is why it\n# can run on a fork's pull request, and why it says \"not your bill\" every\n# time it reports one.\n\non:\n  pull_request:\n\npermissions:\n  contents: read\n  pull-requests: write\n\njobs:\n  estimate:\n    runs-on: ubuntu-latest\n    steps:\n      - uses: actions/checkout@v4\n\n      # Missing secret is a skip with a notice, never a red build. A gate that\n      # fails closed on setup is a gate somebody deletes on their first busy\n      # afternoon.\n      - name: Check for the API key\n        id: key\n        env:\n          ALGOSIZE_API_KEY: ${{ secrets.ALGOSIZE_API_KEY }}\n        run: |\n          if [ -z \"$ALGOSIZE_API_KEY\" ]; then\n            echo \"::notice::ALGOSIZE_API_KEY is not set — skipping the cost estimate.\"\n            echo \"skip=true\" >> \"$GITHUB_OUTPUT\"\n          fi\n\n      - name: Read the budget\n        if: steps.key.outputs.skip != 'true'\n        id: budget\n        run: |\n          COMPOSE=docker-compose.yml\n          CEILING=\n          PROVIDERS='[\"hetzner\",\"aws\"]'\n          if [ -f algosize.budget.json ]; then\n            COMPOSE=$(jq -r '.compose // \"docker-compose.yml\"' algosize.budget.json)\n            CEILING=$(jq -r '.monthlyCeilingUsd // empty' algosize.budget.json)\n            PROVIDERS=$(jq -c '.providers // [\"hetzner\",\"aws\"]' algosize.budget.json)\n          fi\n          echo \"compose=$COMPOSE\"     >> \"$GITHUB_OUTPUT\"\n          echo \"ceiling=$CEILING\"     >> \"$GITHUB_OUTPUT\"\n          echo \"providers=$PROVIDERS\" >> \"$GITHUB_OUTPUT\"\n          if [ ! -f \"$COMPOSE\" ]; then\n            echo \"::notice::No $COMPOSE in this repository — nothing to price.\"\n            echo \"skip=true\" >> \"$GITHUB_OUTPUT\"\n          fi\n\n      - name: Estimate\n        if: steps.key.outputs.skip != 'true' && steps.budget.outputs.skip != 'true'\n        id: run\n        env:\n          ALGOSIZE_API_KEY: ${{ secrets.ALGOSIZE_API_KEY }}\n        run: |\n          jq -n             --rawfile compose \"${{ steps.budget.outputs.compose }}\"             --argjson providers '${{ steps.budget.outputs.providers }}'             '{inputType:\"compose\", content:$compose, options:{providers:$providers}}' > payload.json\n\n          HTTP=$(curl -sS -o response.json -w '%{http_code}'             -X POST \"https://algosize.com/api/estimate\"             -H \"Authorization: Bearer $ALGOSIZE_API_KEY\"             -H \"Content-Type: application/json\"             --data @payload.json)\n\n          if [ \"$HTTP\" != \"200\" ]; then\n            echo \"::warning::The estimator returned HTTP $HTTP — annotating without a verdict.\"\n            jq -r '.message // .error // \"no detail\"' response.json || true\n            echo \"skip=true\" >> \"$GITHUB_OUTPUT\"\n            exit 0\n          fi\n\n          # Cheapest priced provider. Providers that could not be priced are\n          # excluded rather than counted as zero.\n          jq -r '[.providers[] | select(.estimatedTotalMicroUsd != null)]\n                 | sort_by(.estimatedTotalMicroUsd) | .[0]\n                 | \"cheapest_usd=(.estimatedTotalMicroUsd / 1000000)\",\n                   \"cheapest_name=(.providerName // .providerId)\"' response.json >> \"$GITHUB_OUTPUT\"\n          jq -r '\"resources=(.normalizedSpec.resources | length)\"' response.json >> \"$GITHUB_OUTPUT\"\n\n      - name: Comment and gate\n        if: steps.key.outputs.skip != 'true' && steps.budget.outputs.skip != 'true' && steps.run.outputs.skip != 'true'\n        env:\n          GH_TOKEN: ${{ github.token }}\n          CEILING:  ${{ steps.budget.outputs.ceiling }}\n          USD:      ${{ steps.run.outputs.cheapest_usd }}\n          NAME:     ${{ steps.run.outputs.cheapest_name }}\n          RESOURCES: ${{ steps.run.outputs.resources }}\n          PR: ${{ github.event.pull_request.number }}\n        run: |\n          VERDICT=\"No ceiling is set, so this is an annotation only.\"\n          FAIL=0\n          if [ -n \"$CEILING\" ]; then\n            if awk \"BEGIN{exit !($USD > $CEILING)}\"; then\n              VERDICT=\"Over the $$CEILING/mo ceiling.\"\n              FAIL=1\n            else\n              VERDICT=\"Within the $$CEILING/mo ceiling.\"\n            fi\n          fi\n\n          {\n            echo \"<!-- algosize-estimate -->\"\n            echo \"### Infrastructure cost\"\n            echo\n            echo \"**$$USD / month** on $NAME — cheapest of the priced providers, $RESOURCES resources.\"\n            echo\n            echo \"$VERDICT\"\n            echo\n            echo \"_List prices against the committed compose file. Not a quote, and not your bill._\"\n            echo \"_No cloud account was contacted and no credential was used._\"\n          } > comment.md\n\n          gh pr comment \"$PR\" --body-file comment.md --edit-last ||             gh pr comment \"$PR\" --body-file comment.md\n\n          if [ \"$FAIL\" = \"1\" ]; then\n            echo \"::error::Estimated monthly cost $$USD exceeds the $$CEILING ceiling.\"\n            exit 1\n          fi\n"
  },
  "/api/ci/architecture-snippet": {
    "filename": ".github/workflows/algosize-architecture.yml",
    "secretName": "ALGOSIZE_API_KEY",
    "setupSteps": [
      "Use the same ALGOSIZE_API_KEY repository secret the dependency audit uses.",
      "Commit the workflow file below. It annotates by default and fails nothing until you raise arch_fail_on."
    ],
    "workflow": "name: Algosize architecture\n\n# Maps the module graph on every pull request and reports what changed shape.\n# Submits source PATHS and contents to the same endpoint the dependency audit\n# uses; the result is stored as findings and paths, never as a copy of the\n# files.\n#\n# arch_fail_on is \"none\" by default: this annotates and fails nothing. Raise\n# it to \"critical\" once you have watched a few runs and agree with what it\n# calls critical.\n\non:\n  pull_request:\n\npermissions:\n  contents: read\n  pull-requests: write\n\njobs:\n  architecture:\n    runs-on: ubuntu-latest\n    steps:\n      - uses: actions/checkout@v4\n\n      - name: Check for the API key\n        id: key\n        env:\n          ALGOSIZE_API_KEY: ${{ secrets.ALGOSIZE_API_KEY }}\n        run: |\n          if [ -z \"$ALGOSIZE_API_KEY\" ]; then\n            echo \"::notice::ALGOSIZE_API_KEY is not set — skipping the architecture map.\"\n            echo \"skip=true\" >> \"$GITHUB_OUTPUT\"\n          fi\n\n      - name: Collect source\n        if: steps.key.outputs.skip != 'true'\n        id: collect\n        run: |\n          # Source only, and bounded. Build output and vendored dependencies\n          # carry no signal about YOUR architecture and would dominate the graph.\n          find .             -path ./node_modules -prune -o             -path ./.git -prune -o             -path ./dist -prune -o             -path ./build -prune -o             -path ./vendor -prune -o             -type f ( -name '*.js' -o -name '*.mjs' -o -name '*.ts' -o -name '*.tsx' -o -name '*.py' -o -name '*.go' )             -size -200k -print | head -400 | sed 's|^./||' > files.txt\n\n          COUNT=$(wc -l < files.txt | tr -d ' ')\n          echo \"count=$COUNT\" >> \"$GITHUB_OUTPUT\"\n          if [ \"$COUNT\" = \"0\" ]; then\n            echo \"::notice::No source files matched — nothing to map.\"\n            echo \"skip=true\" >> \"$GITHUB_OUTPUT\"\n          fi\n\n      - name: Map\n        if: steps.key.outputs.skip != 'true' && steps.collect.outputs.skip != 'true'\n        id: run\n        env:\n          ALGOSIZE_API_KEY: ${{ secrets.ALGOSIZE_API_KEY }}\n        run: |\n          jq -Rn --arg repo \"${{ github.repository }}\"                  --arg ref \"${{ github.head_ref }}\"                  --arg sha \"${{ github.event.pull_request.head.sha }}\"             '{repo:$repo, ref:$ref, commit_sha:$sha, arch_fail_on:\"none\",\n              files:[inputs | {path:., content:\"\"}]}' < files.txt > skeleton.json\n\n          # Read each file into its entry. Done in jq rather than in the shell\n          # so contents are JSON-escaped exactly once.\n          python3 - <<'PY' > payload.json\n          import json, pathlib\n          skel = json.load(open(\"skeleton.json\"))\n          out = []\n          for entry in skel[\"files\"]:\n              p = pathlib.Path(entry[\"path\"])\n              try:\n                  out.append({\"path\": entry[\"path\"], \"content\": p.read_text(errors=\"replace\")})\n              except OSError:\n                  pass\n          skel[\"files\"] = out\n          json.dump(skel, open(\"/dev/stdout\", \"w\"))\n          PY\n\n          HTTP=$(curl -sS -o response.json -w '%{http_code}'             -X POST \"https://algosize.com/api/ci/runs\"             -H \"Authorization: Bearer $ALGOSIZE_API_KEY\"             -H \"Content-Type: application/json\"             --data @payload.json)\n\n          if [ \"$HTTP\" != \"200\" ]; then\n            echo \"::warning::The architecture endpoint returned HTTP $HTTP — annotating without a verdict.\"\n            echo \"skip=true\" >> \"$GITHUB_OUTPUT\"\n            exit 0\n          fi\n\n          jq -r '.architecture\n                 | \"clusters=(.summary.clusters // 0)\",\n                   \"findings=(.summary.findings // 0)\",\n                   \"worst=(.worstSeverity // \"none\")\",\n                   \"failed=(.failed)\"' response.json >> \"$GITHUB_OUTPUT\"\n\n      - name: Comment\n        if: steps.key.outputs.skip != 'true' && steps.collect.outputs.skip != 'true' && steps.run.outputs.skip != 'true'\n        env:\n          GH_TOKEN: ${{ github.token }}\n          PR: ${{ github.event.pull_request.number }}\n        run: |\n          {\n            echo \"<!-- algosize-architecture -->\"\n            echo \"### Architecture\"\n            echo\n            echo \"${{ steps.collect.outputs.count }} files · ${{ steps.run.outputs.clusters }} clusters · ${{ steps.run.outputs.findings }} findings (worst: ${{ steps.run.outputs.worst }})\"\n          } > comment.md\n          gh pr comment \"$PR\" --body-file comment.md --edit-last ||             gh pr comment \"$PR\" --body-file comment.md\n\n          if [ \"${{ steps.run.outputs.failed }}\" = \"true\" ]; then\n            echo \"::error::Architecture findings exceeded the configured threshold.\"\n            exit 1\n          fi\n"
  },
  "/api/monitors": {
    "monitors": [
      {
        "monitorId": "mon_1",
        "repoUrl": "https://github.com/acme/api-gateway-with-a-long-name",
        "branch": "main",
        "schedule": "daily",
        "lastRunAt": 1785538800,
        "paused": false,
        "createdAt": 1785542400,
        "knownAdvisoryCount": 2,
        "lastDelta": {
          "total": 2,
          "counts": {
            "critical": 1,
            "high": 1
          },
          "at": 1785538800
        },
        "analyzers": [
          "vuln",
          "arch",
          "estimate",
          "algo"
        ],
        "archFindingCount": 3,
        "lastEstimate": {
          "byProvider": {
            "hetzner": 12400000,
            "aws": 41000000
          },
          "at": 1785538800
        },
        "lastAlgo": {
          "functions": 2,
          "at": 1785538800
        },
        "lastStatus": "ok",
        "lastAttemptAt": 1785538800,
        "lastError": null,
        "runAtHour": 14
      },
      {
        "monitorId": "mon_2",
        "repoUrl": "https://github.com/acme/billing-worker",
        "branch": "main",
        "schedule": "daily",
        "lastRunAt": null,
        "paused": false,
        "createdAt": 1785542400,
        "knownAdvisoryCount": null,
        "lastDelta": null,
        "analyzers": [
          "vuln"
        ],
        "archFindingCount": null,
        "lastEstimate": null,
        "lastAlgo": null,
        "lastStatus": null,
        "lastAttemptAt": null,
        "lastError": null,
        "runAtHour": null
      }
    ],
    "monitorLimit": 1,
    "monitorsUsed": 2
  },
  "/api/monitors/route": {
    "channels": [
      {
        "id": "email",
        "label": "Email",
        "wired": true,
        "detail": [
          "dana@acme.test",
          "engineering-oncall@acme.test"
        ],
        "note": null
      },
      {
        "id": "slack",
        "label": "Slack",
        "wired": false,
        "detail": [],
        "note": "No webhook is configured for this organisation."
      }
    ],
    "muted": false,
    "reason": null,
    "summary": "The next alert goes to 2 email addresses."
  },
  "/api/scorecard": {
    "columns": [
      {
        "id": "security",
        "label": "Security"
      },
      {
        "id": "cost",
        "label": "Cost"
      },
      {
        "id": "complexity",
        "label": "Complexity"
      },
      {
        "id": "architecture",
        "label": "Architecture"
      }
    ],
    "rows": [
      {
        "monitorId": "mon_1",
        "repo": "acme/api-gateway-with-a-long-name",
        "repoUrl": "https://github.com/acme/api-gateway-with-a-long-name",
        "branch": "main",
        "paused": false,
        "gradedAt": 1785538800,
        "attemptedAt": 1785538800,
        "status": "ok",
        "error": null,
        "cells": {
          "security": {
            "kind": "grade",
            "value": "F · 2",
            "note": "Worst is critical; 2 advisories total.",
            "rank": 61
          },
          "cost": {
            "kind": "grade",
            "value": "$12.40",
            "note": "Cheapest of 2 providers — hetzner. List prices, not your bill.",
            "rank": 12400000
          },
          "complexity": {
            "kind": "grade",
            "value": "O(n²)",
            "note": "Worst of 2 measured entries.",
            "rank": 4
          },
          "architecture": {
            "kind": "grade",
            "value": "3",
            "note": "3 open findings.",
            "rank": 3
          }
        }
      },
      {
        "monitorId": "mon_2",
        "repo": "acme/billing-worker",
        "repoUrl": "https://github.com/acme/billing-worker",
        "branch": "main",
        "paused": false,
        "gradedAt": null,
        "attemptedAt": null,
        "status": null,
        "error": null,
        "cells": {
          "security": {
            "kind": "pending",
            "note": "No sweep has completed yet.",
            "value": null,
            "rank": null
          },
          "cost": {
            "kind": "off",
            "note": null,
            "value": null,
            "rank": null
          },
          "complexity": {
            "kind": "off",
            "note": null,
            "value": null,
            "rank": null
          },
          "architecture": {
            "kind": "off",
            "note": null,
            "value": null,
            "rank": null
          }
        }
      }
    ],
    "basis": "Rows come from scheduled monitors. A repository has to be under watch to be graded."
  },
  "/api/runs": {
    "items": [
      {
        "id": "run_algo",
        "analyzer": "algo",
        "headline": "O(n²) · 612.40 ms",
        "ms": 640.1,
        "createdAt": 1785542400000,
        "source": "ci",
        "repo": null,
        "commitSha": null,
        "hasInput": true,
        "credentialKind": null
      },
      {
        "id": "run_arch",
        "analyzer": "arch",
        "headline": "0 clusters · 0 findings · 0 crit, 0 high",
        "ms": 18.2,
        "createdAt": 1785542400000,
        "source": "manual",
        "repo": null,
        "commitSha": null,
        "hasInput": true,
        "credentialKind": null
      },
      {
        "id": "run_cost",
        "analyzer": "cost",
        "headline": "23% savings · 2 suggestions",
        "ms": 88.4,
        "createdAt": 1785542400000,
        "source": "manual",
        "repo": null,
        "commitSha": null,
        "hasInput": true,
        "credentialKind": null
      },
      {
        "id": "run_estimate",
        "analyzer": "estimate",
        "headline": "$12.40/mo on Hetzner Cloud · 0 resources · 2 providers",
        "ms": 31.7,
        "createdAt": 1785542400000,
        "source": "manual",
        "repo": null,
        "commitSha": null,
        "hasInput": true,
        "credentialKind": null
      },
      {
        "id": "run_vuln",
        "analyzer": "vuln",
        "headline": "F · 2 advisories, worst critical",
        "ms": 412.5,
        "createdAt": 1785538800000,
        "source": "ci",
        "repo": null,
        "commitSha": null,
        "hasInput": true,
        "credentialKind": null
      }
    ],
    "nextCursor": null
  },
  "/api/mcp/manifest": {
    "name": "algosize",
    "version": "1.0.0",
    "protocolVersions": [
      "2025-06-18",
      "2025-03-26",
      "2024-11-05"
    ],
    "endpoint": "https://algosize.com/api/mcp",
    "instructions": "Algosize analyses codebases and infrastructure: dependency vulnerabilities, cloud and infrastructure cost, algorithmic complexity, and architecture. Analysis tools consume the organisation's monthly run allowance — before starting a new analysis, check algosize_list_runs for a recent result that already answers the question. Read-only tools are free; use them freely. algosize_share_run creates a link that anyone who has it can open, so only call it when the user has asked for something shareable.",
    "groups": [
      {
        "id": "analysis",
        "label": "Analysis",
        "tools": [
          "algosize_analyze_vulnerabilities",
          "algosize_analyze_cost",
          "algosize_analyze_complexity",
          "algosize_analyze_architecture",
          "algosize_estimate_infrastructure",
          "algosize_list_cost_providers",
          "algosize_generate_fix"
        ]
      },
      {
        "id": "runs",
        "label": "Runs & Reports",
        "tools": [
          "algosize_list_runs",
          "algosize_get_run",
          "algosize_get_run_report",
          "algosize_share_run"
        ]
      },
      {
        "id": "posture",
        "label": "Posture",
        "tools": [
          "algosize_get_scorecard",
          "algosize_list_arch_snapshots",
          "algosize_diff_architecture",
          "algosize_get_ci_snippet",
          "algosize_whoami"
        ]
      },
      {
        "id": "monitors",
        "label": "Monitors",
        "tools": [
          "algosize_list_monitors",
          "algosize_create_monitor",
          "algosize_update_monitor",
          "algosize_delete_monitor",
          "algosize_run_monitor_now",
          "algosize_get_monitor_result"
        ]
      }
    ],
    "tools": [
      {
        "name": "algosize_analyze_vulnerabilities",
        "title": "Scan dependencies for vulnerabilities",
        "description": "Audit a project's dependencies against known advisories. Give it a GitHub repository URL and it fetches and parses the lockfiles itself; that is the accurate path and the one to prefer. CONSUMES ONE RUN from the organisation's monthly allowance. Before calling it, check algosize_list_runs for a recent 'vuln' run on the same repository — repeating an audit that has not changed wastes the allowance.",
        "inputSchema": {
          "type": "object",
          "properties": {
            "repoUrl": {
              "type": "string",
              "description": "A GitHub repository URL, e.g. https://github.com/owner/name. Preferred over `files`."
            },
            "files": {
              "type": "array",
              "description": "The files to analyse. Send real file contents, not summaries.",
              "items": {
                "type": "object",
                "properties": {
                  "path": {
                    "type": "string",
                    "description": "Repository-relative path, e.g. \"docker-compose.yml\"."
                  },
                  "content": {
                    "type": "string",
                    "description": "The file's full text."
                  }
                },
                "required": [
                  "path",
                  "content"
                ],
                "additionalProperties": false
              },
              "minItems": 1,
              "maxItems": 400
            }
          },
          "additionalProperties": false
        },
        "outputSchema": {
          "type": "object",
          "properties": {
            "counts": {
              "type": "object"
            },
            "summary": {
              "type": "string"
            },
            "packagesScanned": {
              "type": "number"
            },
            "fixCommand": {
              "type": "string"
            },
            "runId": {
              "type": "string"
            }
          }
        },
        "annotations": {
          "readOnlyHint": false,
          "destructiveHint": false,
          "idempotentHint": false,
          "openWorldHint": false
        },
        "_meta": {
          "algosize/metered": true,
          "algosize/scope": "algosize:analyze",
          "algosize/paidOnly": false
        }
      },
      {
        "name": "algosize_analyze_cost",
        "title": "Analyse cloud spend",
        "description": "Find savings in an existing cloud bill. Takes a list of running services with their monthly cost and returns ranked reduction opportunities. This analyses spend you ALREADY have — to price infrastructure you have not built yet, use algosize_estimate_infrastructure instead. CONSUMES ONE RUN.",
        "inputSchema": {
          "type": "object",
          "properties": {
            "services": {
              "type": "array",
              "description": "The services making up the current bill.",
              "items": {
                "type": "object",
                "properties": {
                  "name": {
                    "type": "string",
                    "description": "Service name, e.g. \"RDS\"."
                  },
                  "monthlyCost": {
                    "type": "number",
                    "description": "Current monthly cost in USD."
                  },
                  "region": {
                    "type": "string"
                  },
                  "utilization": {
                    "type": "number",
                    "description": "Average utilisation 0-1, when known."
                  }
                },
                "required": [
                  "name",
                  "monthlyCost"
                ],
                "additionalProperties": false
              },
              "minItems": 1
            }
          },
          "required": [
            "services"
          ],
          "additionalProperties": false
        },
        "outputSchema": {
          "type": "object",
          "properties": {
            "currentSpend": {
              "type": "number"
            },
            "totalSavingsPct": {
              "type": "number"
            },
            "suggestions": {
              "type": "array"
            }
          }
        },
        "annotations": {
          "readOnlyHint": false,
          "destructiveHint": false,
          "idempotentHint": false,
          "openWorldHint": false
        },
        "_meta": {
          "algosize/metered": true,
          "algosize/scope": "algosize:analyze",
          "algosize/paidOnly": false
        }
      },
      {
        "name": "algosize_analyze_complexity",
        "title": "Measure algorithmic complexity",
        "description": "Run a JavaScript function against generated inputs, measure how its runtime grows, and infer its Big-O complexity, with a suggested rewrite when a better one exists. The code is executed in an isolated sandbox, so it must be self-contained and must not need network or filesystem access. CONSUMES ONE RUN.",
        "inputSchema": {
          "type": "object",
          "properties": {
            "code": {
              "type": "string",
              "description": "A self-contained JavaScript function, including its declaration. No imports, no I/O."
            },
            "sampleInput": {
              "description": "Optional representative input used to seed the measurement."
            }
          },
          "required": [
            "code"
          ],
          "additionalProperties": false
        },
        "outputSchema": {
          "type": "object",
          "properties": {
            "bigO": {
              "type": "string"
            },
            "wallTimeMs": {
              "type": "number"
            },
            "suggestion": {
              "type": "string"
            }
          }
        },
        "annotations": {
          "readOnlyHint": false,
          "destructiveHint": false,
          "idempotentHint": false,
          "openWorldHint": false
        },
        "_meta": {
          "algosize/metered": true,
          "algosize/scope": "algosize:analyze",
          "algosize/paidOnly": false
        }
      },
      {
        "name": "algosize_analyze_architecture",
        "title": "X-ray a system's architecture",
        "description": "Build a dependency graph of a system from its configuration files — wrangler.toml, docker-compose.yml, Kubernetes manifests, Terraform — and report structural findings. Send the config files, not application source: source contributes almost nothing to the graph and costs context. CONSUMES ONE RUN.",
        "inputSchema": {
          "type": "object",
          "properties": {
            "files": {
              "type": "array",
              "description": "The files to analyse. Send real file contents, not summaries.",
              "items": {
                "type": "object",
                "properties": {
                  "path": {
                    "type": "string",
                    "description": "Repository-relative path, e.g. \"docker-compose.yml\"."
                  },
                  "content": {
                    "type": "string",
                    "description": "The file's full text."
                  }
                },
                "required": [
                  "path",
                  "content"
                ],
                "additionalProperties": false
              },
              "minItems": 1,
              "maxItems": 400
            }
          },
          "required": [
            "files"
          ],
          "additionalProperties": false
        },
        "outputSchema": {
          "type": "object",
          "properties": {
            "nodes": {
              "type": "number"
            },
            "edges": {
              "type": "number"
            },
            "findings": {
              "type": "array"
            },
            "complete": {
              "type": "boolean"
            }
          }
        },
        "annotations": {
          "readOnlyHint": false,
          "destructiveHint": false,
          "idempotentHint": false,
          "openWorldHint": false
        },
        "_meta": {
          "algosize/metered": true,
          "algosize/scope": "algosize:analyze",
          "algosize/paidOnly": false
        }
      },
      {
        "name": "algosize_estimate_infrastructure",
        "title": "Estimate infrastructure cost",
        "description": "Price infrastructure from its definition — a Terraform plan, docker-compose file, Kubernetes manifest, or a manual resource list — across cloud providers, using published list prices. This prices what you are ABOUT TO build; to find savings in a bill you already pay, use algosize_analyze_cost. Nothing is contacted at any cloud provider and no credentials are ever needed. CONSUMES ONE RUN.",
        "inputSchema": {
          "type": "object",
          "properties": {
            "inputType": {
              "type": "string",
              "enum": [
                "kubernetes",
                "compose",
                "terraform-plan",
                "manual",
                "normalized"
              ],
              "description": "Which format `content` is in."
            },
            "content": {
              "description": "The plan, manifest or resource list. A string for text formats, an object for JSON ones."
            },
            "providers": {
              "type": "array",
              "items": {
                "type": "string"
              },
              "description": "Optional provider ids to price against. Omit to price all of them."
            },
            "region": {
              "type": "string",
              "description": "Optional region hint, e.g. \"us-east-1\"."
            }
          },
          "required": [
            "inputType",
            "content"
          ],
          "additionalProperties": false
        },
        "outputSchema": {
          "type": "object",
          "properties": {
            "providers": {
              "type": "array"
            },
            "warnings": {
              "type": "array"
            },
            "currency": {
              "type": "string"
            }
          }
        },
        "annotations": {
          "readOnlyHint": false,
          "destructiveHint": false,
          "idempotentHint": false,
          "openWorldHint": false
        },
        "_meta": {
          "algosize/metered": true,
          "algosize/scope": "algosize:analyze",
          "algosize/paidOnly": false
        }
      },
      {
        "name": "algosize_list_cost_providers",
        "title": "List priceable cloud providers",
        "description": "List the cloud providers algosize_estimate_infrastructure can price against, with each one's regions and how fresh its price data is. Free and read-only — call it before an estimate when you need to know which provider ids are valid.",
        "inputSchema": {
          "type": "object",
          "properties": {},
          "additionalProperties": false
        },
        "outputSchema": {
          "type": "object",
          "properties": {
            "providers": {
              "type": "array"
            },
            "catalogVersion": {
              "type": "string"
            }
          }
        },
        "annotations": {
          "readOnlyHint": true,
          "destructiveHint": false,
          "idempotentHint": true,
          "openWorldHint": false
        },
        "_meta": {
          "algosize/metered": false,
          "algosize/scope": "algosize:read",
          "algosize/paidOnly": false
        }
      },
      {
        "name": "algosize_generate_fix",
        "title": "Generate a fix for a finding",
        "description": "Produce a concrete remediation for one finding produced by another Algosize tool. Free — it does NOT consume a run. Pass the finding as it was returned rather than a paraphrase; the generator keys off its exact fields.",
        "inputSchema": {
          "type": "object",
          "properties": {
            "kind": {
              "type": "string",
              "description": "The finding's kind, exactly as returned by the analyzer."
            },
            "finding": {
              "type": "object",
              "description": "The finding object, passed through unchanged."
            }
          },
          "required": [
            "kind",
            "finding"
          ],
          "additionalProperties": false
        },
        "outputSchema": {
          "type": "object",
          "properties": {
            "kind": {
              "type": "string"
            },
            "fix": {}
          }
        },
        "annotations": {
          "readOnlyHint": false,
          "destructiveHint": false,
          "idempotentHint": false,
          "openWorldHint": false
        },
        "_meta": {
          "algosize/metered": false,
          "algosize/scope": "algosize:analyze",
          "algosize/paidOnly": false
        }
      },
      {
        "name": "algosize_list_runs",
        "title": "List recent analysis runs",
        "description": "List this organisation's recent analysis runs, newest first. Free and read-only. Call this BEFORE any metered analysis tool: if a recent run already covers the same repository and analyzer, read it with algosize_get_run instead of paying for a new one.",
        "inputSchema": {
          "type": "object",
          "properties": {
            "limit": {
              "type": "integer",
              "minimum": 1,
              "maximum": 50,
              "description": "How many to return. Default 20."
            },
            "cursor": {
              "type": "string",
              "description": "Pagination cursor from a previous call's nextCursor."
            },
            "analyzer": {
              "type": "string",
              "enum": [
                "cost",
                "vuln",
                "algo",
                "arch",
                "estimate"
              ],
              "description": "Only runs from this analyzer."
            },
            "source": {
              "type": "string",
              "enum": [
                "ci",
                "manual"
              ],
              "description": "Only runs from this origin."
            }
          },
          "additionalProperties": false
        },
        "outputSchema": {
          "type": "object",
          "properties": {
            "items": {
              "type": "array"
            },
            "nextCursor": {
              "type": "string"
            }
          }
        },
        "annotations": {
          "readOnlyHint": true,
          "destructiveHint": false,
          "idempotentHint": true,
          "openWorldHint": false
        },
        "_meta": {
          "algosize/metered": false,
          "algosize/scope": "algosize:read",
          "algosize/paidOnly": false
        }
      },
      {
        "name": "algosize_get_run",
        "title": "Read one analysis run",
        "description": "Fetch the full stored result of a single run by id, including every finding. Free and read-only. Use it to read an existing result rather than re-running the analysis.",
        "inputSchema": {
          "type": "object",
          "properties": {
            "runId": {
              "type": "string",
              "description": "The run id, from algosize_list_runs."
            }
          },
          "required": [
            "runId"
          ],
          "additionalProperties": false
        },
        "outputSchema": {
          "type": "object",
          "properties": {
            "id": {
              "type": "string"
            },
            "analyzer": {
              "type": "string"
            },
            "result": {}
          }
        },
        "annotations": {
          "readOnlyHint": true,
          "destructiveHint": false,
          "idempotentHint": true,
          "openWorldHint": false
        },
        "_meta": {
          "algosize/metered": false,
          "algosize/scope": "algosize:read",
          "algosize/paidOnly": false
        }
      },
      {
        "name": "algosize_get_run_report",
        "title": "Read a run's rendered report",
        "description": "Fetch a run's report. `json` returns the structured result; `sarif`, `cyclonedx` and `csv` are produced for dependency-audit runs only and will be refused for other analyzers. Free and read-only. HTML is deliberately not offered here — ask for json and describe it instead.",
        "inputSchema": {
          "type": "object",
          "properties": {
            "runId": {
              "type": "string"
            },
            "format": {
              "type": "string",
              "enum": [
                "json",
                "sarif",
                "cyclonedx",
                "csv"
              ],
              "description": "Default json. The last three are dependency-audit runs only."
            }
          },
          "required": [
            "runId"
          ],
          "additionalProperties": false
        },
        "outputSchema": {
          "type": "object",
          "properties": {
            "format": {
              "type": "string"
            },
            "report": {}
          }
        },
        "annotations": {
          "readOnlyHint": true,
          "destructiveHint": false,
          "idempotentHint": true,
          "openWorldHint": false
        },
        "_meta": {
          "algosize/metered": false,
          "algosize/scope": "algosize:read",
          "algosize/paidOnly": false
        }
      },
      {
        "name": "algosize_share_run",
        "title": "Create a public link to a report",
        "description": "CREATES A PUBLICLY REACHABLE LINK to this run's report — anyone who has the URL can open it without signing in. Only call this when the user has explicitly asked for something to share externally; never call it to read a report yourself, which algosize_get_run_report does privately. The link expires, and can be revoked from the dashboard.",
        "inputSchema": {
          "type": "object",
          "properties": {
            "runId": {
              "type": "string"
            },
            "expiresInDays": {
              "type": "integer",
              "minimum": 1,
              "maximum": 90,
              "description": "Link lifetime. The server applies its own default and maximum."
            }
          },
          "required": [
            "runId"
          ],
          "additionalProperties": false
        },
        "outputSchema": {
          "type": "object",
          "properties": {
            "url": {
              "type": "string"
            },
            "expiresAt": {
              "type": "number"
            }
          }
        },
        "annotations": {
          "readOnlyHint": false,
          "destructiveHint": false,
          "idempotentHint": false,
          "openWorldHint": true
        },
        "_meta": {
          "algosize/metered": false,
          "algosize/scope": "algosize:manage",
          "algosize/paidOnly": false
        }
      },
      {
        "name": "algosize_get_scorecard",
        "title": "Read the engineering scorecard",
        "description": "One graded row per monitored repository across security, cost, complexity and architecture. Free and read-only, and the fastest way to see overall posture without running anything. Each cell reports its own kind: `grade` is a real result, `stale` is an old one, `pending` means the first run has not finished, and `off` means that analyzer is not enabled — treat those four as genuinely different, never as a missing score.",
        "inputSchema": {
          "type": "object",
          "properties": {},
          "additionalProperties": false
        },
        "outputSchema": {
          "type": "object",
          "properties": {
            "rows": {
              "type": "array"
            },
            "columns": {
              "type": "array"
            }
          }
        },
        "annotations": {
          "readOnlyHint": true,
          "destructiveHint": false,
          "idempotentHint": true,
          "openWorldHint": false
        },
        "_meta": {
          "algosize/metered": false,
          "algosize/scope": "algosize:read",
          "algosize/paidOnly": false
        }
      },
      {
        "name": "algosize_list_arch_snapshots",
        "title": "List architecture snapshots",
        "description": "List stored architecture snapshots for this organisation, newest first. Each snapshot is one captured graph; pairs of them are what algosize_diff_architecture compares. Free and read-only.",
        "inputSchema": {
          "type": "object",
          "properties": {
            "repoUrl": {
              "type": "string",
              "description": "Filter to one repository. Pass an empty string for manual uploads, which have no repository."
            },
            "branch": {
              "type": "string"
            },
            "limit": {
              "type": "integer",
              "minimum": 1,
              "maximum": 200
            }
          },
          "additionalProperties": false
        },
        "outputSchema": {
          "type": "object",
          "properties": {
            "snapshots": {
              "type": "array"
            }
          }
        },
        "annotations": {
          "readOnlyHint": true,
          "destructiveHint": false,
          "idempotentHint": true,
          "openWorldHint": false
        },
        "_meta": {
          "algosize/metered": false,
          "algosize/scope": "algosize:read",
          "algosize/paidOnly": false
        }
      },
      {
        "name": "algosize_diff_architecture",
        "title": "Diff two architecture snapshots",
        "description": "Compare an architecture snapshot against an earlier one and report which components and dependencies were added or removed — the 'did this change add a dependency on the payments database' question. Pass only `to` to compare it against whatever preceded it. Free and read-only. A result with comparable=false means there was nothing to compare against, which is NOT the same as no changes.",
        "inputSchema": {
          "type": "object",
          "properties": {
            "to": {
              "type": "string",
              "description": "The snapshot to diff."
            },
            "from": {
              "type": "string",
              "description": "Optional baseline. Defaults to the snapshot recorded before `to`."
            }
          },
          "required": [
            "to"
          ],
          "additionalProperties": false
        },
        "outputSchema": {
          "type": "object",
          "properties": {
            "diff": {
              "type": "object"
            },
            "note": {
              "type": "string"
            }
          }
        },
        "annotations": {
          "readOnlyHint": true,
          "destructiveHint": false,
          "idempotentHint": true,
          "openWorldHint": false
        },
        "_meta": {
          "algosize/metered": false,
          "algosize/scope": "algosize:read",
          "algosize/paidOnly": false
        }
      },
      {
        "name": "algosize_get_ci_snippet",
        "title": "Get a CI workflow snippet",
        "description": "Return a ready-to-commit GitHub Actions workflow that runs one Algosize check on every pull request. Free and read-only. The snippet references a repository secret by name and never contains a key — the user creates the key themselves in the dashboard.",
        "inputSchema": {
          "type": "object",
          "properties": {
            "kind": {
              "type": "string",
              "enum": [
                "audit",
                "optimizer",
                "estimate",
                "architecture"
              ],
              "description": "Which gate: audit (dependencies), optimizer, estimate (cost), or architecture."
            },
            "failOn": {
              "type": "string",
              "enum": [
                "critical",
                "high",
                "medium",
                "low",
                "none"
              ],
              "description": "audit only — severity at which the build fails. Default high."
            }
          },
          "required": [
            "kind"
          ],
          "additionalProperties": false
        },
        "outputSchema": {
          "type": "object",
          "properties": {
            "filename": {
              "type": "string"
            },
            "workflow": {
              "type": "string"
            },
            "secretName": {
              "type": "string"
            }
          }
        },
        "annotations": {
          "readOnlyHint": true,
          "destructiveHint": false,
          "idempotentHint": true,
          "openWorldHint": false
        },
        "_meta": {
          "algosize/metered": false,
          "algosize/scope": "algosize:read",
          "algosize/paidOnly": false
        }
      },
      {
        "name": "algosize_whoami",
        "title": "Show the connected account",
        "description": "Report which Algosize account this connection is acting as, its plan, and how much of the monthly run allowance is left. Free and read-only. Call it first when a metered tool is refused, to see whether the allowance is the reason.",
        "inputSchema": {
          "type": "object",
          "properties": {},
          "additionalProperties": false
        },
        "outputSchema": {
          "type": "object",
          "properties": {
            "plan": {
              "type": "string"
            },
            "active": {
              "type": "boolean"
            },
            "monthlyRunsUsed": {
              "type": "number"
            },
            "monthlyRunsLimit": {
              "type": "number"
            },
            "authMethod": {
              "type": "string"
            }
          }
        },
        "annotations": {
          "readOnlyHint": true,
          "destructiveHint": false,
          "idempotentHint": true,
          "openWorldHint": false
        },
        "_meta": {
          "algosize/metered": false,
          "algosize/scope": "algosize:read",
          "algosize/paidOnly": false
        }
      },
      {
        "name": "algosize_list_monitors",
        "title": "List scheduled monitors",
        "description": "List the repositories under scheduled watch, with each one's schedule, enabled analyzers, last result and health. Free and read-only.",
        "inputSchema": {
          "type": "object",
          "properties": {},
          "additionalProperties": false
        },
        "outputSchema": {
          "type": "object",
          "properties": {
            "monitors": {
              "type": "array"
            },
            "monitorsUsed": {
              "type": "number"
            }
          }
        },
        "annotations": {
          "readOnlyHint": true,
          "destructiveHint": false,
          "idempotentHint": true,
          "openWorldHint": false
        },
        "_meta": {
          "algosize/metered": false,
          "algosize/scope": "algosize:read",
          "algosize/paidOnly": false
        }
      },
      {
        "name": "algosize_create_monitor",
        "title": "Watch a repository on a schedule",
        "description": "Put a GitHub repository under scheduled watch so the chosen analyzers run automatically and alert on new findings. Creating a monitor does not itself consume a run; each scheduled sweep it later performs does. Requires a paid plan once the free monitor limit is reached.",
        "inputSchema": {
          "type": "object",
          "properties": {
            "repoUrl": {
              "type": "string",
              "description": "https://github.com/owner/name"
            },
            "branch": {
              "type": "string"
            },
            "schedule": {
              "type": "string",
              "enum": [
                "daily",
                "weekly"
              ],
              "description": "Default daily."
            },
            "runAtHour": {
              "type": "integer",
              "minimum": 0,
              "maximum": 23,
              "description": "UTC hour to run at. Omit for any sweep."
            },
            "analyzers": {
              "type": "array",
              "items": {
                "type": "string",
                "enum": [
                  "vuln",
                  "arch",
                  "estimate",
                  "algo"
                ]
              },
              "description": "Which analyzers this monitor runs."
            }
          },
          "required": [
            "repoUrl"
          ],
          "additionalProperties": false
        },
        "outputSchema": {
          "type": "object",
          "properties": {
            "monitor": {
              "type": "object"
            }
          }
        },
        "annotations": {
          "readOnlyHint": false,
          "destructiveHint": false,
          "idempotentHint": false,
          "openWorldHint": false
        },
        "_meta": {
          "algosize/metered": false,
          "algosize/scope": "algosize:manage",
          "algosize/paidOnly": false
        }
      },
      {
        "name": "algosize_update_monitor",
        "title": "Change a monitor's analyzers, schedule or paused state",
        "description": "Change one existing monitor: which analyzers it runs, when it runs, or whether it is paused. Supply only the fields you are changing. Pausing stops future sweeps without losing history.",
        "inputSchema": {
          "type": "object",
          "properties": {
            "monitorId": {
              "type": "string"
            },
            "analyzers": {
              "type": "array",
              "items": {
                "type": "string",
                "enum": [
                  "vuln",
                  "arch",
                  "estimate",
                  "algo"
                ]
              }
            },
            "schedule": {
              "type": "string",
              "enum": [
                "daily",
                "weekly"
              ]
            },
            "runAtHour": {
              "type": "integer",
              "minimum": 0,
              "maximum": 23
            },
            "paused": {
              "type": "boolean"
            }
          },
          "required": [
            "monitorId"
          ],
          "additionalProperties": false
        },
        "outputSchema": {
          "type": "object",
          "properties": {
            "monitor": {
              "type": "object"
            },
            "applied": {
              "type": "array"
            }
          }
        },
        "annotations": {
          "readOnlyHint": false,
          "destructiveHint": false,
          "idempotentHint": false,
          "openWorldHint": false
        },
        "_meta": {
          "algosize/metered": false,
          "algosize/scope": "algosize:manage",
          "algosize/paidOnly": false
        }
      },
      {
        "name": "algosize_delete_monitor",
        "title": "Stop watching a repository",
        "description": "Permanently remove a monitor. The repository stops being analysed on a schedule and stops alerting. Past run history is kept. If the intent is a temporary stop, use algosize_update_monitor with paused=true instead — that is reversible and this is not.",
        "inputSchema": {
          "type": "object",
          "properties": {
            "monitorId": {
              "type": "string"
            }
          },
          "required": [
            "monitorId"
          ],
          "additionalProperties": false
        },
        "outputSchema": {
          "type": "object",
          "properties": {
            "removed": {
              "type": "boolean"
            }
          }
        },
        "annotations": {
          "readOnlyHint": false,
          "destructiveHint": true,
          "idempotentHint": true,
          "openWorldHint": false
        },
        "_meta": {
          "algosize/metered": false,
          "algosize/scope": "algosize:manage",
          "algosize/paidOnly": false
        }
      },
      {
        "name": "algosize_run_monitor_now",
        "title": "Run a monitor immediately",
        "description": "Queue an out-of-schedule sweep for one monitor. Returns as soon as the work is queued, not when it finishes — read the result afterwards with algosize_get_monitor_result. The sweep itself consumes runs from the monthly allowance, one per enabled analyzer, even though this call does not. Refused if the monitor is paused.",
        "inputSchema": {
          "type": "object",
          "properties": {
            "monitorId": {
              "type": "string"
            }
          },
          "required": [
            "monitorId"
          ],
          "additionalProperties": false
        },
        "outputSchema": {
          "type": "object",
          "properties": {
            "queued": {
              "type": "boolean"
            }
          }
        },
        "annotations": {
          "readOnlyHint": false,
          "destructiveHint": false,
          "idempotentHint": false,
          "openWorldHint": false
        },
        "_meta": {
          "algosize/metered": false,
          "algosize/scope": "algosize:manage",
          "algosize/paidOnly": false
        }
      },
      {
        "name": "algosize_get_monitor_result",
        "title": "Read a monitor's latest analyzer result",
        "description": "Read the most recent result one analyzer produced for one monitored repository, with its baseline and the delta since. Free and read-only. The reply's `status` distinguishes a real result from an analyzer that is not enabled and from one whose first run has not landed — do not read the last two as 'nothing found'.",
        "inputSchema": {
          "type": "object",
          "properties": {
            "monitorId": {
              "type": "string"
            },
            "analyzer": {
              "type": "string",
              "enum": [
                "vuln",
                "arch",
                "estimate",
                "algo"
              ]
            }
          },
          "required": [
            "monitorId",
            "analyzer"
          ],
          "additionalProperties": false
        },
        "outputSchema": {
          "type": "object",
          "properties": {
            "status": {
              "type": "string"
            },
            "result": {},
            "delta": {}
          }
        },
        "annotations": {
          "readOnlyHint": true,
          "destructiveHint": false,
          "idempotentHint": true,
          "openWorldHint": false
        },
        "_meta": {
          "algosize/metered": false,
          "algosize/scope": "algosize:read",
          "algosize/paidOnly": false
        }
      }
    ],
    "prompts": [
      {
        "name": "audit_repository",
        "title": "Audit a repository end to end",
        "description": "Dependency scan, architecture x-ray, then a prioritised list of what to fix first.",
        "arguments": [
          {
            "name": "repoUrl",
            "description": "https://github.com/owner/name",
            "required": true
          },
          {
            "name": "branch",
            "description": "Branch to audit. Defaults to the repository's default branch.",
            "required": false
          }
        ]
      },
      {
        "name": "explain_findings",
        "title": "Explain a run's findings",
        "description": "Turn one analysis run into an explanation pitched at a chosen audience.",
        "arguments": [
          {
            "name": "runId",
            "description": "The run to explain.",
            "required": true
          },
          {
            "name": "audience",
            "description": "engineer or exec. Defaults to engineer.",
            "required": false
          }
        ]
      },
      {
        "name": "pre_release_check",
        "title": "Pre-release gate",
        "description": "A pass/fail summary suitable for pasting into a pull request.",
        "arguments": [
          {
            "name": "repoUrl",
            "description": "https://github.com/owner/name",
            "required": true
          }
        ]
      }
    ]
  },
  "/api/mcp/usage": {
    "calls": [],
    "totals": {
      "calls": 0,
      "ok": 0,
      "quotaRefused": 0,
      "runsStarted": 0,
      "avgDurationMs": null,
      "busiestTool": null,
      "errorRate": null
    },
    "comparable": true,
    "since": 1782950400
  },
  "/api/mcp/clients": {
    "connections": []
  },
  "__runs": [
    {
      "id": "run_arch",
      "analyzer": "arch"
    },
    {
      "id": "run_algo",
      "analyzer": "algo"
    },
    {
      "id": "run_estimate",
      "analyzer": "estimate"
    },
    {
      "id": "run_cost",
      "analyzer": "cost"
    },
    {
      "id": "run_vuln",
      "analyzer": "vuln"
    }
  ],
  "/api/runs/run_arch": {
    "id": "run_arch",
    "userId": "usr_fixture",
    "orgId": "org_fixture",
    "source": "manual",
    "analyzer": "arch",
    "input": {
      "fileCount": 3,
      "paths": [
        "src/index.js",
        "src/api.js",
        "src/db.js"
      ]
    },
    "result": {
      "graph": {
        "nodes": [],
        "edges": [],
        "clusters": []
      },
      "findings": [],
      "recommendations": [],
      "summary": {
        "clusters": 0,
        "nodes": 0,
        "edges": 0,
        "findings": 0,
        "byLens": {
          "speed": 0,
          "cost": 0,
          "security": 0
        },
        "bySeverity": {
          "critical": 0,
          "high": 0,
          "medium": 0,
          "low": 0
        },
        "complete": true
      },
      "limits": {
        "filesAnalyzed": 3,
        "filesSkipped": 0,
        "skipped": [],
        "truncatedSkippedList": false,
        "oversized": [],
        "droppedForMissingEvidence": 0,
        "notImplemented": [
          {
            "lens": "cost",
            "rule": "egress_volume",
            "why": "Egress cost depends on bytes transferred, which no file in a repository records. The nearest detectable proxy — one external API called from several clusters — is reported as `shared_external_dependency` instead, and is about coupling, not bytes."
          },
          {
            "lens": "speed",
            "rule": "chatty_edge_by_volume",
            "why": "Call volume is a runtime property. What is counted here is distinct call SITES between one pair of services, which is a code-shape signal, not traffic."
          }
        ]
      }
    },
    "ms": 18.2,
    "headline": "0 clusters · 0 findings · 0 crit, 0 high",
    "createdAt": 1785542400000
  },
  "/api/runs/run_algo": {
    "id": "run_algo",
    "userId": "usr_fixture",
    "orgId": "org_fixture",
    "source": "ci",
    "analyzer": "algo",
    "input": {
      "name": "groupByOwner",
      "ceiling": "O(n log n)"
    },
    "result": {
      "bigO": {
        "label": "O(n²)",
        "confidence": "high"
      },
      "wallTimeMs": 612.4,
      "samples": [
        {
          "n": 100,
          "ms": 0.42
        },
        {
          "n": 200,
          "ms": 1.61
        },
        {
          "n": 400,
          "ms": 6.38
        },
        {
          "n": 800,
          "ms": 25.9
        }
      ],
      "notes": "Timings taken on a shared runner; treat a one-bucket move as noise."
    },
    "ms": 640.1,
    "headline": "O(n²) · 612.40 ms",
    "createdAt": 1785542400000
  },
  "/api/runs/run_estimate": {
    "id": "run_estimate",
    "userId": "usr_fixture",
    "orgId": "org_fixture",
    "source": "manual",
    "analyzer": "estimate",
    "input": {
      "inputType": "compose",
      "resourceCount": 3
    },
    "result": {
      "providers": [
        {
          "providerId": "hetzner",
          "providerName": "Hetzner Cloud",
          "estimatedTotalMicroUsd": 12400000,
          "confidence": "medium"
        },
        {
          "providerId": "aws",
          "providerName": "Amazon Web Services",
          "estimatedTotalMicroUsd": 41000000,
          "lowerBoundMicroUsd": 36000000,
          "upperBoundMicroUsd": 48500000,
          "confidence": "low"
        }
      ],
      "resourceCount": 3,
      "warningCount": 2,
      "duration": "1mo",
      "currency": "USD",
      "pricingCatalogVersion": "2026-08",
      "inputType": "compose",
      "disclaimer": "List prices against the submitted specification. Not a quote, and not your bill.",
      "specRetained": false,
      "specNote": "The submitted specification was not retained. Only per-provider totals and a resource count are kept — see handlers/estimate_history.js."
    },
    "ms": 31.7,
    "headline": "$12.40/mo on Hetzner Cloud · 0 resources · 2 providers",
    "createdAt": 1785542400000
  },
  "/api/runs/run_cost": {
    "id": "run_cost",
    "userId": "usr_fixture",
    "orgId": "org_fixture",
    "source": "manual",
    "analyzer": "cost",
    "input": {
      "file": "cur-2026-08.csv"
    },
    "result": {
      "totalSavingsPct": 23,
      "suggestions": [
        {
          "title": "Rightsize 4 over-provisioned EC2 instances",
          "service": "EC2",
          "monthlySavingsUsd": 412.9,
          "severity": "high",
          "detail": "Four m5.2xlarge instances averaged 8% CPU across the billing period.",
          "action": "Move to m5.large and re-measure after a full week."
        },
        {
          "title": "Delete 1.2 TB of unattached EBS volumes",
          "service": "EBS",
          "monthlySavingsUsd": 96,
          "severity": "medium",
          "detail": "Eleven volumes have had no attachment for the whole period.",
          "action": "Snapshot, then delete."
        }
      ]
    },
    "ms": 88.4,
    "headline": "23% savings · 2 suggestions",
    "createdAt": 1785542400000
  },
  "/api/runs/run_vuln": {
    "id": "run_vuln",
    "userId": "usr_fixture",
    "orgId": "org_fixture",
    "source": "ci",
    "analyzer": "vuln",
    "input": {
      "repoUrl": "https://github.com/acme/api-gateway-with-a-long-name"
    },
    "result": {
      "advisories": [
        {
          "id": "GHSA-a",
          "package": "lodash",
          "ecosystem": "npm",
          "severity": "critical",
          "installedVersion": "4.17.11",
          "fixedIn": "4.17.21",
          "summary": "Prototype pollution in lodash"
        },
        {
          "id": "GHSA-b",
          "package": "axios",
          "ecosystem": "npm",
          "severity": "high",
          "installedVersion": "0.21.0",
          "fixedIn": "0.21.2",
          "summary": "SSRF in axios"
        }
      ],
      "summary": {
        "securityScore": 39,
        "grade": "F",
        "totalIssues": 2,
        "counts": {
          "critical": 1,
          "high": 1,
          "medium": 0,
          "low": 0,
          "unknown": 0
        },
        "worstSeverity": "critical",
        "sourceFindings": 0,
        "dependencyAdvisories": 2,
        "remediation": [],
        "complete": true
      }
    },
    "ms": 412.5,
    "headline": "F · 2 advisories, worst critical",
    "createdAt": 1785538800000
  }
}
