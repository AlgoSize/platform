# site/

Jekyll project for the Algosize marketing site and dashboard.

## Run locally

```bash
cd site
bundle install
bundle exec jekyll serve --host 0.0.0.0 --port 5000 --livereload
```

Output is built to `_site/`.
